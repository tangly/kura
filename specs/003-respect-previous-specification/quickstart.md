# Quickstart

This feature involves a global theme update and does not have a specific, isolated entry point for a quickstart guide. 

To see the changes, simply run the application after the implementation is complete. The new theme will be applied automatically across the entire app. You can test the light and dark modes by changing your device's system settings.
